<header class="top-header<% if (is_post()) { %> in-post<% } %><% if (page.layout === 'blog') { %> in-home<% } %>" id="header">
    <div class="flex-row">
        <a href="javascript:;" class="header-icon waves-effect waves-circle waves-light on" id="menu-toggle">
          <i class="icon icon-lg icon-navicon"></i>
        </a>
        <div class="flex-col"></div>
        <% if(theme.search){ %>
        <div class="search-wrap" id="search-wrap">
            <a href="javascript:;" class="header-icon waves-effect waves-circle waves-light" id="back">
                <i class="icon icon-lg icon-chevron-left"></i>
            </a>
            <input type="text" id="key" class="search-input" autocomplete="off" placeholder="<%= __('global.search_input_hint') %>">
            <a href="javascript:;" class="header-icon waves-effect waves-circle waves-light" id="search">
                <i class="icon icon-lg icon-search"></i>
            </a>
        </div>
        <% }%>
    </div>
    <div class="header-title ellipsis"><%=title %></div>
</header>
<header class="content-header <%-hdClass %>">
    <% if (page.layout === 'blog' || is_post() || page.layout === 'tags') { %>
    <canvas class="flickering-grid-canvas" aria-hidden="true"></canvas>
    <% } %>
    <div class="container fade-scale">
        <div id="myheader">
            <h1 class="title">
                <% if(is_home() || page.layout === 'blog' || page.layout === 'gallery'){ %> 
                    <%=title %>
                <% } %>
            </h1>
            <h5 class="subtitle">
                <% if(is_post()){ %>
                <% } else if(is_home()){ %>
                    <%= theme.homesubtitle %>
                <% } else if (page.layout === 'blog' && page.description) { %>
                    <%- page.description %>
                <% } else if (page.layout === 'gallery' && page.description) { %>
                    <%- page.description %>
                <% } else if(is_archive()){ %>
                    <h1 class="archivestitle"><%= theme.archivestitle %></h1>
                <% } else if (page.layout === 'page' && page.description) {%>
                    <%- page.description %>
                <% } else { %>
                    <h1 id="pagetitle" class="archivestitle"></h1>
                    <script>
                        if(location.href.indexOf('about')!=-1){
                            pagetitle.textContent = '<%= theme.aboutitle %>';
                        }else if(location.href.indexOf('tags')!=-1||location.href.indexOf('categories')!=-1){
                            var a=location.href.substring(location.href.length-5,location.href.length-1);
                            if(a==='tags'){
                                pagetitle.textContent = '<%= theme.tagstitle %>';
                            }else if(a==='ries'){
                                pagetitle.textContent = '<%= theme.categoriestitle %>';
                            }
                        }
                    </script>
                <% } %>
            </h5>
        </div>
    </div>

</header>
